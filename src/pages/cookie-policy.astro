---
import StandardPage from "@/components/StandardPage.astro";
import type { Locale } from "@/data/i18n";

type Props = {
  locale?: Locale;
};

const { locale = "en" } = Astro.props;
---

<StandardPage page="cookie-policy" locale={locale} />
