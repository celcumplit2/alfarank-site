---
import CasesIndexPage from "@/pages/cases/index.astro";

export function getStaticPaths() {
  return [{ params: { locale: "ru" }, props: { locale: "ru" } }];
}

const { locale } = Astro.props;
---

<CasesIndexPage locale={locale} />
